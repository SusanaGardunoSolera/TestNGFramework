/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package com.framework.app;

import com.framework.list.LinkedList;

import static com.framework.utilities.StringUtils.join;
import static com.framework.utilities.StringUtils.split;
import static com.framework.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
